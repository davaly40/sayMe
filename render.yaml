services:
  - type: web
    name: sayme
    env: python
    buildCommand: pip install -r backend/requirements.txt
    startCommand: uvicorn backend.main:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.12
